{% extends 'back/base.html.twig' %}

{% block title %}PORTFOLIO - Edit techno{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('collection') }}
{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-5">
        <div class="col-2">
        </div>
        <div class="col-8">
            <h1 class="text-center">Modifier la techno</h1>
            <h3 class="text-center mb-3">{{ techno.name }}</h3>
            
            {{ include('back/techno/_form.html.twig') }}
        </div>
        <div class="col-2 mt-3">
            <a href="{{ path('admin_techno_browse') }}"><button type="submit" class="btn btn-warning">Retour</button></a>
        </div>
    </div>
</div>
{% endblock %}